apiVersion: kubeflow.org/v1
kind: TFJob
metadata:
  name: rasa-tfjob-cnd-paradise-88467603765-0
  namespace: tenant-7654b5-pilot
spec:
  tfReplicaSpecs:
    PS:
      replicas: 1
      restartPolicy: OnFailure
      template:
        metadata:
          annotations:
            sidecar.istio.io/inject: 'false'
          labels:
            app: shopify-trainer
        spec:
          containers:
          - args:
            - pwd && curl https://zeniva-shopify-ord1.object.ord1.coreweave.com/shopify/models/data-cnd-paradise-88467603765-0.json
              --output data-cnd-paradise-88467603765-0.json && ls
            command:
            - /bin/sh
            - -c
            image: python:3.10
            imagePullPolicy: Always
            name: datafetch
            ports:
            - containerPort: 5005
              name: tfjob-port
            resources:
              limits:
                cpu: 1
                memory: 1Gi
            volumeMounts:
            - mountPath: /app
              name: rasa-data
            workingDir: /app
          imagePullSecrets:
          - name: docker-secret
          volumes:
          - name: rasa-data
            persistentVolumeClaim:
              claimName: rasa-data
    Worker:
      replicas: 1
      restartPolicy: OnFailure
      template:
        metadata:
          annotations:
            sidecar.istio.io/inject: 'false'
        spec:
          containers:
          - args:
            - pwd && cd / && ls && python test.py /app/data-cnd-paradise-88467603765-0.json
            command:
            - /bin/sh
            - -c
            image: exartadev/zeniva.exarta:tfjobgputest30
            imagePullPolicy: IfNotPresent
            name: tensorflow
            ports:
            - containerPort: 5005
              name: tfjob-port
            resources:
              limits:
                cpu: 4
                memory: 16Gi
                nvidia.com/gpu: 1
            volumeMounts:
            - mountPath: /app
              name: rasa-data
            workingDir: /app
          imagePullSecrets:
          - name: docker-secret
          volumes:
          - name: rasa-data
            persistentVolumeClaim:
              claimName: rasa-data
